// Minimal integration test scaffold.
// TODO: Add fixtures under tests/fixtures/ and call into core/probes with mocked inputs.
#[test]
fn workspace_compiles() {
    //TODO: This test exists just to make `cargo test` run something until real tests are implemented.
    assert!(true);
}
